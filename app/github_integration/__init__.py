"""GitHub-модуль. Fine-grained токен: Contents (rw) + Administration
(только смена видимости), без delete_repo.

Жёсткое ограничение уровня кода, не просто UI-запрет (см. README):
удаление репозитория и истории коммитов недостижимо ни через один путь
в кодовой базе. Поэтому в этом пакете НЕТ и не должно появляться метода,
вызывающего delete на репозиторий — ни напрямую, ни через generic
passthrough к PyGithub Repository.delete().
"""
