# AGENTS.md — pointer file (LLM-ONLY, English on purpose)

This project's AI rules live in exactly one place: **CLAUDE.md**, at the repo root.

This file exists only because some tools (Codex CLI and others) look for `AGENTS.md` by convention instead of
`CLAUDE.md`. It is intentionally NOT a second copy — a duplicate drifts the moment one copy is edited and the other
is not (see CLAUDE.md's own "Doc-writing rules": never duplicate a definition, only point to it).

If you are an agent that reads this file: read `CLAUDE.md` now and follow it exactly, including its POINTERS section
to `AI_COMMANDS.md`, `CHEK_PROTOCOL.md`, `DELEGATION.md`, `BRANCHING.md`, `TOKEN_ECONOMY.md` and `PROJECT_MEMORY.md`.
Nothing in this repo's AI-collaboration model differs by which entry file you found first.
